import { environment } from 'src/environments/environment';
export const studentEndPoint = {
  list: `${environment.url}/students`,
  detail: `${environment.url}/students`,
  create: `${environment.url}/students`,
  update: `${environment.url}/students`,
  delete: `${environment.url}/students`,
};
